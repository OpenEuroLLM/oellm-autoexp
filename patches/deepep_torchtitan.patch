diff --git a/torchtitan/distributed/expert_parallel.py b/torchtitan/distributed/expert_parallel.py
index 8ee53e75..edb671f1 100644
--- a/torchtitan/distributed/expert_parallel.py
+++ b/torchtitan/distributed/expert_parallel.py
@@ -320,8 +320,8 @@ class ReordererSequenceParallel(ParallelStyle):
 class DeepEPExpertParallel(BaseExpertParallel):
     """Expert Parallel using DeepEP for efficient token dispatch/combine.

-    Expects inputs as:
-        (hidden_states, num_tokens_per_expert, selected_experts_indices, top_scores, num_experts)
+    Expects inputs as (hidden_states, num_tokens_per_expert) with routing
+    context stored on the module as ``_deepep_routing_ctx`` by DeepEPMoE.

     Args:
         score_before_experts: If True, apply routing scores before expert computation.
@@ -336,7 +336,11 @@ class DeepEPExpertParallel(BaseExpertParallel):
         """Dispatch tokens via DeepEP."""
         from torchtitan.distributed.deepep import dispatch_tokens

-        hidden_states, _, selected_experts_indices, top_scores, num_experts = inputs
+        hidden_states, _ = inputs
+        ctx = mod._deepep_routing_ctx
+        selected_experts_indices = ctx["selected_experts_indices"]
+        top_scores = ctx["top_scores"]
+        num_experts = ctx["num_experts"]
         if isinstance(mod.w1, DTensor):
             num_local_experts = mod.w1.to_local().shape[0]
         else:
diff --git a/torchtitan/models/moe/moe_deepep.py b/torchtitan/models/moe/moe_deepep.py
index 54e3f0f2..b1373c5c 100644
--- a/torchtitan/models/moe/moe_deepep.py
+++ b/torchtitan/models/moe/moe_deepep.py
@@ -42,14 +42,15 @@ class DeepEPMoE(MoE):
             with torch.no_grad():
                 self.tokens_per_expert.add_(num_tokens_per_expert)

-        # Call experts with routing info - hooks handle DeepEP dispatch/combine
-        routed_output = self.experts(
-            x,
-            num_tokens_per_expert,
-            selected_experts_indices,
-            top_scores,
-            self.experts.num_experts,
-        )
+        # Store routing context on the experts module for DeepEPExpertParallel
+        # hooks to retrieve (avoids passing 5 args through GroupedExperts.forward
+        # which only accepts 2 and breaks PP shape inference).
+        self.experts._deepep_routing_ctx = {
+            "selected_experts_indices": selected_experts_indices,
+            "top_scores": top_scores,
+            "num_experts": self.experts.num_experts,
+        }
+        routed_output = self.experts(x, num_tokens_per_expert)

         out = self.shared_experts(x) if self.shared_experts is not None else None
