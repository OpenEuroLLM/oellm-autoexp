defaults:
  - project: default
  - backend: megatron_torchrun
  - container: none
  - slurm: base
  - sweep: none
  - scheduler: default
  - job: default
  - _self_


index: 0   # will be replaced internally by overrides for a sweep
stage: ""  # will be replaced internally by overrides for a sweep
