# Data inputs
data_path:
  - /flash/project_462000963/users/niccolo/copy_nemotron_downsampled_15p/high-all
data_cache_path: ${oc.env:DATA_DIR}/.cache
split: "989,10,1"
lr_warmup_iters: 0
train_iters: 500
save_interval: 1000000000
save: ${job.base_output_dir}
load: ${job.base_output_dir}
tensorboard_dir: ${job.base_output_dir}/tensorboard
