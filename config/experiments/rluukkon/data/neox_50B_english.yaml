# @package backend.megatron
# Dataset shape and tokenizer settings for neox-50B English data.
# Cluster-specific data_path / data_cache_path live in the experiment's
# cluster config (e.g. moe_2B_400M/mi325x.yaml).
global_batch_size: 1024
seq_length: 4096

max_position_embeddings: ${.seq_length}
num_workers: 7
num_dataset_builder_threads: 7
split: '99,1,0'
create_attention_mask_in_dataloader: False
# data_args_path: local/6040_datapath.txt

# Tokenizer
tokenizer_model: EleutherAI/gpt-neox-20b
tokenizer_type: HuggingFaceTokenizer

# DATA
micro_batch_size: 8

