# @package _global_
# Isolate overlap_param_gather effect while keeping overlap_grad_reduce enabled.
defaults:
  - qwen3_30B_A3B_memory_debug_mock
  - _self_

backend:
  megatron:
    overlap_param_gather: false
    overlap_grad_reduce: true

job:
  name: qwen3_30B_A3B_memory_debug_mock_no_param_gather${backend.megatron.lr}_${slurm.env.MACHINE_NAME}
