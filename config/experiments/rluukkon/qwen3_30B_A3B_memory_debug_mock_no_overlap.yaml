# @package _global_
# Mock-data overlay variant to test overlap communication impact on RSS growth.
defaults:
  - qwen3_30B_A3B_memory_debug_mock
  - _self_

backend:
  megatron:
    overlap_param_gather: false
    overlap_grad_reduce: false

job:
  name: qwen3_30B_A3B_memory_debug_mock_no_overlap${backend.megatron.lr}_${slurm.env.MACHINE_NAME}
