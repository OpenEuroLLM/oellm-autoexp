# @package _global_
# Megatron 235BA22B — Risto's H100 reference EXACT on JUPITER (64 nodes, 256 GPUs)
#
# Source: risto_qwen3_235BA22B_config.sh (ModelZoo H100 baseline)
# Original: TP=2, PP=8, VPP=4, EP=32 on 256 GPUs
#
# On JUPITER 64 nodes (256 GPUs): EXACT MATCH to H100 reference parallelism!
#   DP = 256 / (2×8) = 16. expert_dp = 256 / (1×32×8) = 1.
#   GBS=2048, MBS=1: num_micro = 2048/16 = 128.
#   VPP=4: 3 layers/virtual stage. Min microbatches = 8. 128 >> 8.
#
# H100 reference achieved 245-276 TFLOPS (245 base, 276 with 1F1B overlap).
# JUPITER GH200 target: scaled proportionally to GH200 peak (similar to H100 SXM).
#
# ── Results (64 nodes, 256×GH200) ────────────────────────────────────────────
# ALL CRASHED — DeepEP/HybridEP NVSHMEM failures on JUPITER GH200.
# No training iterations completed.

defaults:
  - ../../base
  - /backend: megatron_torchrun_localaddr
  - /container: jupiter
  - /slurm: jupiter
  - /backend/megatron: [base, qwen3_moe_235BA22B]
  - /sweep: none
  - _self_

aux:
  experiment_group: "megatron_moe_235BA22B_risto_64node"
  tag: "hybridep_ep32"

backend:
  megatron:
    train_iters: 50
    log_interval: 1
    eval_interval: 1000
    save: null
    load: null
    ckpt_step: 0
    log_throughput: true
    exit_interval: 50

    # ═══ EXACT Risto parallelism ═══
    tensor_model_parallel_size: 2
    pipeline_model_parallel_size: 8
    expert_model_parallel_size: 32
    expert_tensor_parallel_size: 1
    context_parallel_size: 1
    global_batch_size: 2048
    micro_batch_size: 1

    account_for_embedding_in_pipeline_split: true
    account_for_loss_in_pipeline_split: true
    num_virtual_stages_per_pipeline_rank: 4

    # ═══ Flex dispatcher — HybridEP ═══
    moe_token_dispatcher_type: flex
    moe_flex_dispatcher_backend: hybridep
    moe_hybridep_num_sms: 16
    moe_grouped_gemm: true
    moe_permute_fusion: true
    moe_router_fusion: true

    # ═══ Selective recompute ═══
    recompute_granularity: selective
    recompute_modules:
      - moe_act
      - layernorm
    recompute_method: null
    recompute_num_layers: null

    # ═══ All three overlaps ═══
    overlap_grad_reduce: true
    overlap_param_gather: true
    overlap_moe_expert_parallel_comm: true

    # ═══ Attention + compute ═══
    attention_backend: fused
    sequence_parallel: true
    cross_entropy_loss_fusion: true
    cross_entropy_fusion_impl: te
    enable_experimental: true
    create_attention_mask_in_dataloader: false

    # ═══ Router ═══
    moe_router_dtype: fp32
    moe_router_load_balancing_type: aux_loss
    moe_aux_loss_coeff: 0.001
    moe_z_loss_coeff: 1e-4

    # ═══ Optimizer ═══
    use_distributed_optimizer: true
    lr: 3e-4
    min_lr: 0
    adam_beta1: 0.9
    adam_beta2: 0.95

    # ═══ Memory ═══
    manual_gc: true
    manual_gc_interval: 5

    # ═══ Data — mock ═══
    mock_data: true
    seq_length: 4096
    max_position_embeddings: 4096

    # ═══ Misc ═══
    te_rng_tracker: true
    cuda_graph_impl: "none"
    cuda_graph_scope: "full"
    make_vocab_size_divisible_by: 256
    dataloader_type: single

  env:
    TOKENIZERS_PARALLELISM: "false"
    TRANSFORMERS_CACHE: ${oc.env:HF_HOME}/transformers
    HUGGINGFACE_HUB_CACHE: ${oc.env:HF_HOME}/hub
    HF_HOME: ${oc.env:HF_HOME}
    CUDA_DEVICE_MAX_CONNECTIONS: "32"
    NVTE_FWD_LAYERNORM_SM_MARGIN: "24"
    NVTE_BWD_LAYERNORM_SM_MARGIN: "24"
    NCCL_NVLS_ENABLE: "1"
    NVTE_ALLOW_NONDETERMINISTIC_ALGO: "1"
    PYTHONPATH: ".:submodules/Megatron-LM:/opt/"
    TRITON_LIBCUDA_PATH: "/opt/nvidia/lib"
    PATH: "/usr/local/bin:/usr/bin:/bin"
    CUDNN_PATH: "/usr/lib/aarch64-linux-gnu"
    NCCL_NET_GDR_LEVEL: "0"
    PYTHONWARNINGS: "ignore::UserWarning"

container:
  image: /e/scratch/projectnucleus/poeppel1/container_cache/MegatronTraining-JUPITER-deepep-nvshmem_base2510_aarch64_202603312032.sif
  bind:
    - /e
    - /dev/shm
    - /usr/lib64/libcuda.so.1:/opt/nvidia/lib/libcuda.so.1:ro
    - /usr/lib64/libcuda.so:/opt/nvidia/lib/libcuda.so:ro
    - /usr/lib64/libnvidia-ml.so.1:/opt/nvidia/lib/libnvidia-ml.so.1:ro
    - /usr/lib64/libnvidia-ptxjitcompiler.so.1:/opt/nvidia/lib/libnvidia-ptxjitcompiler.so.1:ro

slurm:
  sbatch:
    nodes: 64
    time: "00:30:00"
  env:
    PYTORCH_CUDA_ALLOC_CONF: ""
    UB_SKIPMC: "1"
    NCCL_NET_GDR_LEVEL: "0"

job:
  name: meg_risto_235BA22B_64n_${aux.tag}
  base_output_dir: "${oc.env:OUTPUT_DIR,'./output'}/${aux.experiment_group}/${job.name}"

sweep:
  type: list
  store_sweep_json: true
  groups:
    - type: list
      configs:
        # ═══════════════════════════════════════════════════════════════════════
        # Exact H100 reference (TP=2, PP=8, EP=32)
        # ═══════════════════════════════════════════════════════════════════════

        # ── HybridEP EP=32 (exact Risto config) ──────────────────────────
        - aux.tag: hybridep_ep32
          backend.megatron.moe_flex_dispatcher_backend: hybridep

        # ── DeepEP EP=32 (NVSHMEM alternative) ───────────────────────────
        - aux.tag: deepep_ep32
          backend.megatron.moe_flex_dispatcher_backend: deepep

        # ── HybridEP EP=32 + delay_wgrad (full overlap from reference) ──
        - aux.tag: hybridep_ep32_delaywgrad
          backend.megatron.moe_flex_dispatcher_backend: hybridep
          backend.megatron.delay_wgrad_compute: true

        # ── HybridEP EP=32 + force_load_balancing (risk test) ────────────
        - aux.tag: hybridep_ep32_forcebal
          backend.megatron.moe_flex_dispatcher_backend: hybridep
          backend.megatron.moe_router_force_load_balancing: true
