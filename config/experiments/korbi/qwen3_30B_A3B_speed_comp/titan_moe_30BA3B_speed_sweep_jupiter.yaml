# @package _global_
# MoE speed sweep for Titan Qwen3 30BA3B on JUPITER (2 nodes, 8 GPUs)
#
# Architecture: 48 layers, dim=2048, 32 heads, 4 KV heads (GQA), 128 experts, topk=8
# Parallelism: EP=8, dp_shard=8 (FSDP for attention; efsdp=1 for experts)
#   EP is carved from dp_shard: dp_replicate(1)*dp_shard(8)*tp(1)*pp(1) = 8 = world_size
#   efsdp = dp_shard*tp/(etp*ep) = 8*1/(1*8) = 1 → no expert FSDP; experts fully on owning GPU
#
# Baseline (nocompile, full AC, bs=4): 179.72 TFLOPS, 12.25 GiB (13%)
#   → Very low memory: experts+attention FSDP-sharded across 8 GPUs, full AC discards activations.
#   → Huge headroom: ~80 GiB free — try compile, no/selective AC, larger batches.
#
# ── Measured results (2 nodes, 8×GH200, EP=8, dp_shard=8) ───────────────────
#   nocompile, full AC,  bs=4  → 180.04 TFLOPS,  12.25 GiB (13%)  (baseline)
#   compile,   full AC,  bs=4  → 179.83 TFLOPS,  12.25 GiB (13%)  (~same as nocompile — compile barely helps for MoE)
#   compile,   full AC,  bs=8  → 203.12 TFLOPS,  23.35 GiB (25%)
#   nocompile, no AC,    bs=4  → 224.37 TFLOPS,  19.29 GiB (20%)  (+25% vs full AC)
#   compile,   no AC,    bs=4  → 226.94 TFLOPS,  19.29 GiB (20%)
#   compile,   no AC,    bs=8  → 254.74 TFLOPS,  37.43 GiB (39%)
#   nocompile, no AC,    bs=16 → 262.61 TFLOPS,  74.03 GiB (78%)
#   compile,   no AC,    bs=16 → 263.11 TFLOPS,  74.03 GiB (78%)  ← OPTIMAL (+3.3% vs bs=8)
#   compile,   no AC,    bs=32 → OOM (73 GiB used + 24.56 GiB needed > 95 GiB)
defaults:
  - ../../base
  - /backend: titan
  - /container: jupiter_titan
  - /slurm: jupiter
  - /sweep: none
  - override /backend/titan/data: nemonicco_jupiter
  - override /backend/titan/model: qwen3_moe_30BA3B
  - override /job: default
  - _self_

aux:
  experiment_group: "titan_moe_30BA3B_speed_sweep"
  compile_tag: "compile"

backend:
  titan:
    model:
      vocab_size: 50304
      tokenizer_path: /e/project1/projectnucleus/franke5/workspace/titan-train/torchtitan/assets/tokenizer/gpt-neox-20b

    training:
      steps: 100
      local_batch_size: 4
      seq_len: 4096
      dtype: bfloat16

    metrics:
      log_freq: 10

    parallelism:
      tensor_parallel_degree: 1
      pipeline_parallel_degree: 1
      expert_parallel_degree: 8
      data_parallel_replicate_degree: 1
      data_parallel_shard_degree: 8
      context_parallel_degree: 1

    activation_checkpoint:
      mode: full

    benchmarks:
      wikitext2_path: /e/project1/projectnucleus/franke5/data/benchmarks/neox/wikitext2/wikitext2
      wikitext103_path: /e/project1/projectnucleus/franke5/data/benchmarks/neox/wikitext103/wikitext103
      lambada_path: /e/project1/projectnucleus/franke5/data/benchmarks/neox/lambada/lambada

  env:
    TORCHDYNAMO_DISABLE: "1"
    PYTHONPATH: .:./submodules/titan-oellm:./submodules/titan-oellm/torchtitan
    NCCL_SOCKET_FAMILY: "AF_INET"
    GLOO_SOCKET_FAMILY: "AF_INET"
    NCCL_SOCKET_IFNAME: "ib0"
    GLOO_SOCKET_IFNAME: "ib0"
    NCCL_SOCKET_TIMEOUT: "60000"
    NCCL_TIMEOUT: "1800"
    NCCL_IB_TIMEOUT: "100"
    NCCL_IB_RETRY_CNT: "20"
    NCCL_ALGO: "Ring"
    HF_ALLOW_CODE_EVAL: "1"
    TORCH_HOME: ${oc.env:TORCH_HOME,"/tmp/torch_home"}
    PYTORCH_KERNEL_CACHE_PATH: ${oc.env:PYTORCH_KERNEL_CACHE_PATH,"/tmp/torch_home/kernels"}
    TRITON_CACHE_DIR: ${oc.env:TRITON_CACHE_DIR,"/tmp/triton_cache"}
    TORCHINDUCTOR_CACHE_DIR: ${oc.env:TORCHINDUCTOR_CACHE_DIR,"/tmp/torchinductor_cache"}
    TORCHINDUCTOR_FX_GRAPH_CACHE: ${oc.env:TORCHINDUCTOR_FX_GRAPH_CACHE,"/tmp/torchinductor_fx_cache"}
    TORCHINDUCTOR_AUTOGRAD_CACHE: ${oc.env:TORCHINDUCTOR_AUTOGRAD_CACHE,"/tmp/torchinductor_autograd_cache"}
    TORCH_INDUCTOR_CUDAGRAPH_DISABLE: ${oc.env:TORCH_INDUCTOR_CUDAGRAPH_DISABLE,"1"}
    PYTORCH_CUDA_ALLOC_CONF: ${oc.env:PYTORCH_CUDA_ALLOC_CONF,"expandable_segments:True"}
    TRITON_LIBCUDA_PATH: "/opt/nvidia/lib"

  torchrun_args:
    nproc_per_node: ${slurm.sbatch.gpus_per_node}
    nnodes: ${slurm.sbatch.nodes}
    max_restarts: 0
    rdzv_backend: "c10d"
    rdzv_endpoint: "$MASTER_ADDR:$MASTER_PORT"
    node_rank: "$SLURM_PROCID"
    master_addr: "$MASTER_ADDR"
    master_port: "$MASTER_PORT"

job:
  name: titan_moe_30BA3B_ep8_${aux.compile_tag}_ac${backend.titan.activation_checkpoint.mode}_bs${backend.titan.training.local_batch_size}
  base_output_dir: "${oc.env:OUTPUT_DIR,'./output'}/${aux.experiment_group}/${job.name}"

container:
  bind:
    - /e/
    - /usr/lib64/libcuda.so.1:/opt/nvidia/lib/libcuda.so.1:ro
    - /usr/lib64/libcuda.so:/opt/nvidia/lib/libcuda.so:ro
    - /usr/lib64/libnvidia-ml.so.1:/opt/nvidia/lib/libnvidia-ml.so.1:ro
    - /usr/lib64/libnvidia-ptxjitcompiler.so.1:/opt/nvidia/lib/libnvidia-ptxjitcompiler.so.1:ro

slurm:
  template_path: "templates/jupiter_mod.sbatch"
  sbatch:
    nodes: 2
    gpus_per_node: 4
    gres: "gpu:4"
    cpus_per_task: 72
    threads_per_core: 1
    account: ${oc.env:SLURM_ACCOUNT,e-sta-openeurollm}
    partition: booster
    time: "00:30:00"
  srun_opts: "--nodes=$SLURM_NNODES --gres=gpu:4 --cpus-per-task=72 --wait=20 --kill-on-bad-exit=1 --label "
  env:
    NUM_NODES: "$SLURM_JOB_NUM_NODES"
    GPUS_PER_NODE: "4"
    NUM_GPUS_PER_NODE: "4"
    NUM_GPUS: "$((NUM_GPUS_PER_NODE*SLURM_NNODES))"
    SLURM_CPUS_PER_TASK: "72"

sweep:
  type: list
  store_sweep_json: true
  groups:
    - type: list
      configs:
        # ── nocompile baseline ────────────────────────────────────────────────
        - aux.compile_tag: nocompile
          backend.titan.activation_checkpoint.mode: full
          backend.titan.training.local_batch_size: 4
          backend.env.TORCHDYNAMO_DISABLE: "1"
          # Result: 180.04 TFLOPS, 12.25 GiB (13%) (job 305636) — baseline

        # ── compile (barely helps for MoE: EP routing overhead dominates) ────
        - aux.compile_tag: compile
          backend.titan.activation_checkpoint.mode: full
          backend.titan.training.local_batch_size: 4
          # Result: 179.83 TFLOPS, 12.25 GiB (13%) (job 305639) — ~same as nocompile

        - aux.compile_tag: compile
          backend.titan.activation_checkpoint.mode: full
          backend.titan.training.local_batch_size: 8
          # Result: 203.12 TFLOPS, 23.35 GiB (25%) (job 305641)

        # ── no AC (19 GiB at bs=4, 37 GiB at bs=8, 74 GiB at bs=16) ────────
        - aux.compile_tag: nocompile
          backend.titan.activation_checkpoint.mode: none
          backend.titan.training.local_batch_size: 4
          backend.env.TORCHDYNAMO_DISABLE: "1"
          # Result: 224.37 TFLOPS, 19.29 GiB (20%) (job 305637)

        - aux.compile_tag: compile
          backend.titan.activation_checkpoint.mode: none
          backend.titan.training.local_batch_size: 4
          # Result: 226.94 TFLOPS, 19.29 GiB (20%) (job 305634)

        - aux.compile_tag: compile
          backend.titan.activation_checkpoint.mode: none
          backend.titan.training.local_batch_size: 8
          # Result: 254.74 TFLOPS, 37.43 GiB (39%) (job 305635)

        # ── larger batches ────────────────────────────────────────────────────
        - aux.compile_tag: nocompile
          backend.titan.activation_checkpoint.mode: none
          backend.titan.training.local_batch_size: 16
          backend.env.TORCHDYNAMO_DISABLE: "1"
          # Result: 262.61 TFLOPS, 74.03 GiB (78%) (job 305633)

        - aux.compile_tag: compile
          backend.titan.activation_checkpoint.mode: none
          backend.titan.training.local_batch_size: 16
          # Result: 263.11 TFLOPS, 74.03 GiB (78%) (job 305638) ← OPTIMAL

        - aux.compile_tag: compile
          backend.titan.activation_checkpoint.mode: none
          backend.titan.training.local_batch_size: 32
          # Result: OOM — 73 GiB in use + 24.56 GiB needed > 95 GiB (job 305640)
