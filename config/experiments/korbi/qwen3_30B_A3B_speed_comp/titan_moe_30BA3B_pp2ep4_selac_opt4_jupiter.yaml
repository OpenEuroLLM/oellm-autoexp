# @package _global_
# Titan MoE 30BA3B PP=2 EP=4 — selective-AC option=4 ceiling push
#
# opt=2 (every 2nd layer recomputed): 129 TFLOPS, 77 GiB (81%)
# opt=3 (every 3rd layer recomputed): 136-137 TFLOPS, 82 GiB (86%) ← BEST SO FAR
#
# Recompute fractions (24 layers per PP stage):
#   opt=2: 12/24 = 50% recomputed  → 75-77 GiB
#   opt=3:  8/24 = 33% recomputed  → 82 GiB (+5 GiB vs opt=2)
#   opt=4:  6/24 = 25% recomputed  → expected ~85-87 GiB (+3-5 GiB vs opt=3)
#   opt=6:  4/24 = 17% recomputed  → expected ~88-92 GiB
#   no-AC:  0/24 = 0%  recomputed  → OOM (even at bs=4)
#
# Expected throughput gains (vs opt=3 = 136 TFLOPS):
#   opt=4: -8pp recompute → expected +2-4% → ~139-141 TFLOPS
#   If OOM at opt=4: opt=3 is the practical ceiling with current settings.
#
# 1F1B memory model: only 1 microbatch activations in-flight at any time.
#   Memory increase opt=3→opt=4 ≈ 4 extra layer activations per microbatch.
#   At ~2 GiB per layer group, expect +3-4 GiB → ~85-87 GiB total (89-92%).
#   Should fit within 95 GiB.
#
# Also testing opt3+bs=128: bubble 0.78% vs 1.54% at bs=64 → +0.8% marginal gain.
#   Memory stays flat (1F1B) at ~82 GiB. Step time ~2× longer: ~54s/step.
#   Worth testing the +1 TFLOPS gain given experiment is cheap.

defaults:
  - ../../base
  - /backend: titan
  - /container: jupiter_titan
  - /slurm: jupiter
  - /sweep: none
  - override /backend/titan/data: nemonicco_jupiter
  - override /backend/titan/model: qwen3_moe_30BA3B
  - override /job: default
  - _self_

aux:
  experiment_group: "titan_moe_30BA3B_pp2ep4_selac_opt4"
  compile_tag: "compile"
  ac_opt_tag: "opt3"

backend:
  titan:
    model:
      vocab_size: 50304
      tokenizer_path: /e/project1/projectnucleus/franke5/workspace/titan-train/torchtitan/assets/tokenizer/gpt-neox-20b

    training:
      steps: 100
      local_batch_size: 64
      seq_len: 4096
      dtype: bfloat16

    metrics:
      log_freq: 10

    parallelism:
      tensor_parallel_degree: 1
      pipeline_parallel_degree: 2
      expert_parallel_degree: 4
      data_parallel_replicate_degree: 1
      data_parallel_shard_degree: 4
      context_parallel_degree: 1

    activation_checkpoint:
      mode: selective
      selective_ac_option: "3"

    benchmarks:
      wikitext2_path: /e/project1/projectnucleus/franke5/data/benchmarks/neox/wikitext2/wikitext2
      wikitext103_path: /e/project1/projectnucleus/franke5/data/benchmarks/neox/wikitext103/wikitext103
      lambada_path: /e/project1/projectnucleus/franke5/data/benchmarks/neox/lambada/lambada

  env:
    TORCHDYNAMO_DISABLE: "0"
    PYTHONPATH: .:./submodules/titan-oellm:./submodules/titan-oellm/torchtitan
    NCCL_SOCKET_FAMILY: "AF_INET"
    GLOO_SOCKET_FAMILY: "AF_INET"
    NCCL_SOCKET_IFNAME: "ib0"
    GLOO_SOCKET_IFNAME: "ib0"
    NCCL_SOCKET_TIMEOUT: "60000"
    NCCL_TIMEOUT: "1800"
    NCCL_IB_TIMEOUT: "100"
    NCCL_IB_RETRY_CNT: "20"
    NCCL_NET_GDR_LEVEL: "0"
    HF_ALLOW_CODE_EVAL: "1"
    TORCH_HOME: ${oc.env:TORCH_HOME,"/tmp/torch_home"}
    PYTORCH_KERNEL_CACHE_PATH: ${oc.env:PYTORCH_KERNEL_CACHE_PATH,"/tmp/torch_home/kernels"}
    TRITON_CACHE_DIR: ${oc.env:TRITON_CACHE_DIR,"/tmp/triton_cache"}
    TORCHINDUCTOR_CACHE_DIR: ${oc.env:TORCHINDUCTOR_CACHE_DIR,"/tmp/torchinductor_cache"}
    TORCHINDUCTOR_FX_GRAPH_CACHE: ${oc.env:TORCHINDUCTOR_FX_GRAPH_CACHE,"/tmp/torchinductor_fx_cache"}
    TORCHINDUCTOR_AUTOGRAD_CACHE: ${oc.env:TORCHINDUCTOR_AUTOGRAD_CACHE,"/tmp/torchinductor_autograd_cache"}
    TORCH_INDUCTOR_CUDAGRAPH_DISABLE: ${oc.env:TORCH_INDUCTOR_CUDAGRAPH_DISABLE,"1"}
    PYTORCH_CUDA_ALLOC_CONF: ${oc.env:PYTORCH_CUDA_ALLOC_CONF,"expandable_segments:True"}
    TRITON_LIBCUDA_PATH: "/opt/nvidia/lib"

  torchrun_args:
    nproc_per_node: ${slurm.sbatch.gpus_per_node}
    nnodes: ${slurm.sbatch.nodes}
    max_restarts: 0
    rdzv_backend: "c10d"
    rdzv_endpoint: "$MASTER_ADDR:$MASTER_PORT"
    node_rank: "$SLURM_PROCID"
    master_addr: "$MASTER_ADDR"
    master_port: "$MASTER_PORT"

job:
  name: titan_moe_30BA3B_ep4pp2_${aux.compile_tag}_acselective_${aux.ac_opt_tag}_bs${backend.titan.training.local_batch_size}
  base_output_dir: "${oc.env:OUTPUT_DIR,'./output'}/${aux.experiment_group}/${job.name}"

container:
  bind:
    - /e/
    - /usr/lib64/libcuda.so.1:/opt/nvidia/lib/libcuda.so.1:ro
    - /usr/lib64/libcuda.so:/opt/nvidia/lib/libcuda.so:ro
    - /usr/lib64/libnvidia-ml.so.1:/opt/nvidia/lib/libnvidia-ml.so.1:ro
    - /usr/lib64/libnvidia-ptxjitcompiler.so.1:/opt/nvidia/lib/libnvidia-ptxjitcompiler.so.1:ro

slurm:
  template_path: "templates/jupiter_mod.sbatch"
  sbatch:
    nodes: 2
    gpus_per_node: 4
    gres: "gpu:4"
    cpus_per_task: 72
    threads_per_core: 1
    account: ${oc.env:SLURM_ACCOUNT,e-sta-openeurollm}
    partition: booster
    time: "01:00:00"
  srun_opts: "--nodes=$SLURM_NNODES --gres=gpu:4 --cpus-per-task=72 --wait=20 --kill-on-bad-exit=1 --label "
  env:
    NUM_NODES: "$SLURM_JOB_NUM_NODES"
    GPUS_PER_NODE: "4"
    NUM_GPUS_PER_NODE: "4"
    NUM_GPUS: "$((NUM_GPUS_PER_NODE*SLURM_NNODES))"
    SLURM_CPUS_PER_TASK: "72"

sweep:
  type: list
  store_sweep_json: true
  groups:
    - type: list
      configs:
        # ── Config 0: compile + selective-AC option=4 + bs=64 ──────────────────
        # Less recompute: checkpoint every 4th layer (25% recomputed).
        # Expected: ~+3% over opt=3 (136 TFLOPS) → ~139-141 TFLOPS.
        # Memory: ~85-87 GiB (89-92%) — should fit.
        - aux.compile_tag: compile
          aux.ac_opt_tag: opt4
          backend.titan.activation_checkpoint.selective_ac_option: "4"
          backend.titan.training.local_batch_size: 64
          backend.env.TORCHDYNAMO_DISABLE: "0"

        # ── Config 1: compile + selective-AC option=3 + bs=128 ─────────────────
        # bs=128 with opt=3: bubble 0.78% vs 1.54% at bs=64 → +0.8% marginal gain.
        # Memory stays flat (1F1B) at ~82 GiB. Step time ~54s/step.
        # Expected: 136 × 1.008 ≈ 137 TFLOPS.
        - aux.compile_tag: compile
          aux.ac_opt_tag: opt3
          backend.titan.activation_checkpoint.selective_ac_option: "3"
          backend.titan.training.local_batch_size: 128
          backend.env.TORCHDYNAMO_DISABLE: "0"

        # ── Config 2: nocompile + selective-AC option=4 + bs=64 ────────────────
        # Baseline for opt=4 without compile. Reference point.
        # Expected: ~133-135 TFLOPS (less recompute than opt=3=130 TFLOPS nocompile).
        - aux.compile_tag: nocompile
          aux.ac_opt_tag: opt4
          backend.titan.activation_checkpoint.selective_ac_option: "4"
          backend.titan.training.local_batch_size: 64
          backend.env.TORCHDYNAMO_DISABLE: "1"
